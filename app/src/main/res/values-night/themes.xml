<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
     https://material.io/develop/android/theming/dark
    -->

    <!-- TermuxActivity DayNight NoActionBar theme. -->
    <!-- See https://developer.android.com/training/material/theme.html for how to customize the Material theme. -->
    <!-- NOTE: Cannot use "Light." since it hides the terminal scrollbar on the default black background. -->
    <style name="Theme.TermuxActivity.DayNight.NoActionBar" parent="Theme.TermuxApp.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>

        <!-- Splash screen background -->
        <item name="android:windowBackground">@android:color/system_neutral1_900</item>

        <!-- Avoid action mode toolbar pushing down terminal content when
             selecting text on pre-6.0 (non-floating toolbar). -->
        <item name="android:windowActionModeOverlay">true</item>

        <!-- Transparent Status Bar and NavigationBar -->
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>

        <!-- https://developer.android.com/training/tv/start/start.html#transition-color -->
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>

        <!-- Left drawer. -->
        <item name="buttonBarButtonStyle">@style/TermuxActivity.Drawer.ButtonBarStyle.Dark</item>
        <item name="termuxActivityDrawerBackground">@android:color/system_neutral1_900</item>
        <item name="termuxActivityDrawerImageTint">@android:color/system_accent1_10</item>

        <!-- Extra keys colors. -->
        <item name="extraKeysButtonTextColor">@android:color/system_accent1_10</item>
        <item name="extraKeysButtonActiveTextColor">@android:color/system_accent3_10</item>
        <item name="extraKeysButtonBackgroundColor">@android:color/system_neutral1_900</item>
        <item name="extraKeysButtonActiveBackgroundColor">@android:color/system_accent2_600</item>

        <item name="cardBackgroundColor">@android:color/system_neutral1_900</item>
        <item name="android:itemBackground">@android:color/system_neutral1_900</item>


    </style>

</resources>
